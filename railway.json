{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python manage.py migrate --settings=delivery_tracker.settings_production && python manage.py collectstatic --noinput --settings=delivery_tracker.settings_production && python manage.py createsuperuser --settings=delivery_tracker.settings_production --noinput --username admin --email admin@example.com && gunicorn delivery_tracker.wsgi:application --bind 0.0.0.0:$PORT --env DJANGO_SETTINGS_MODULE=delivery_tracker.settings_production"
  }
}
